namespace com.leegm.common.protocol.common;

enum Result:byte{
	SUCCESS,
	ERROR
}

enum Method:byte{
	NONE,
	CREATE,
	READ,
	UPDATE,
	DELETE
}

enum Type:byte{
	PLAYER,
	NPC,
	OBJECT
}

enum State:byte{
	IDLE,
	MOVE,
	ATTACK,
	DEAD
}

enum Event:byte{
    UP,
    DOWN,
    LEFT,
    RIGHT
}

struct Item{
	templateId:ulong;
	amount:ulong;
}

struct Vec3{
	x:float;
	y:float;
	z:float;
}